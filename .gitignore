# Generated at build time — run: node build/convert-filters.js
filter-lists/rules.json

# web-ext packaging output
web-ext-artifacts/

node_modules/

# graphify (read-only codebase analysis) — never commit its output
graphify-out/
